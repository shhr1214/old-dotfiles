;;;; 20-helm.el --- Haskellに関する設定

;;; Commentary:
;; https://github.com/emacs-helm/helm
(add-to-list 'load-path "~/.emacs.d/site-list/async")
(add-to-list 'load-path "~/.emacs.d/site-list/helm")
(require 'helm-config)
(helm-mode 1)

;;; Code:

;;; 20-helm.el ends here
